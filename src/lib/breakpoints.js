/**
 * Breakpoint constants — single source of truth for the JS call sites
 * that need to branch on viewport width (matchMedia/innerWidth checks).
 * Mirrors Tailwind's default `md` boundary (768px) and tailwind.config.js.
 */
export const BREAKPOINT_MD = 768;
